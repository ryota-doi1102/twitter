import { Button } from '@mui/material';
import { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <main>
      <Button variant="contained">Login</Button>
    </main>
  );
};

export default Home;
